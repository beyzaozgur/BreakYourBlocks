import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: '#eceff1',
        padding: 5,
        margin: 5,
        marginTop: 50,
        borderRadius: 5,
    }
});